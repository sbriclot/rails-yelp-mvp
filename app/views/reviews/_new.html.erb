<h4>Add a new review</h4>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Add review", class: "btn btn-primary"%>
  <!-- <%= link_to "Cancel", restaurant_path(@restaurant), class: "btn btn-danger" %> -->
<% end %>